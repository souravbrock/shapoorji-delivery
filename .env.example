# Copy this file to .env and set the URL where your Node API is reachable.
#
# Local development (server/ running on its default port):
# VITE_API_URL=http://localhost:3001
#
# Production (once the Node app is set up in cPanel, typically proxied
# under /api on the same domain, or on its own subdomain):
VITE_API_URL=https://spdelivery.reddevils.co.in
